<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://bmin.ai/feed.xml" rel="self" type="application/atom+xml" /><link href="https://bmin.ai/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-01-21T10:23:29+00:00</updated><id>https://bmin.ai/feed.xml</id><title type="html">Benjamin Minixhofer</title><subtitle>Benjamin Minixhofer&apos;s personal website. I am a machine learning and natural language processing researcher.
</subtitle><entry><title type="html">Ingredients for Successful Retrofitting</title><link href="https://bmin.ai/retrofitting/" rel="alternate" type="text/html" title="Ingredients for Successful Retrofitting" /><published>2025-01-21T00:00:00+00:00</published><updated>2025-01-21T00:00:00+00:00</updated><id>https://bmin.ai/retrofitting</id><content type="html" xml:base="https://bmin.ai/retrofitting/"><![CDATA[<p>Most of my research so far has been on changing the architecture of an existing language model after it has already been trained (called <em>retrofitting</em>), often via an additional training procedure. I view retrofitting as a superset of fine-tuning, where we are not only allowed to change the values of the model parameters, but also which parameters there are.</p>

<p>Retrofitting is useful in a bunch of different areas. For example, language models can be retrofitted to lower-precision parameters via <a href="https://pytorch.org/blog/quantization-aware-training">Quantization-Aware Training (QAT)</a>, to use a more lightweight KV cache via <a href="https://arxiv.org/abs/2506.05345">KV cache sparsification</a>, or to predict multiple future tokens concurrently via <a href="https://arxiv.org/abs/2511.11346">Multi-Token Prediction</a>.</p>

<p>My own research usually involved retrofitting to change the tokenizer, for example to <a href="https://arxiv.org/abs/2405.07883">an arbitrary unseen tokenizer</a> or recently to <a href="https://arxiv.org/abs/2512.15586">byte-level tokenization</a>.</p>

<p>Retrofitting is very cool! And more people should use it to do research. Retrofitting enables GPU poor people (like most of us) to do model training research without falling very far off the frontier of the best open models since the training cost is a small fraction of the cost of training a new model from scratch. Even if you do have the budget to train from scratch, retrofitting could give you a substantially faster pace of new insights.</p>

<p>Over time, I’ve noticed a couple of common ingredients across successful retrofitting projects, which I wanted to share here: self-distillation, architecture-space graduality, sufficient expressivity, and data coverage.</p>

<h1 id="self-distillation">Self-Distillation</h1>

<p><img src="/assets/img/retrofitting_selfdistill.jpg" width="100%" /></p>

<p><a href="https://www.cs.cornell.edu/~caruana/compression.kdd06.pdf">Distillation</a> transfers knowledge from a teacher model to a student model. In the LLM case, this is often done by minimizing the KL divergence between their token predictions. <em>Self-distillation</em> is distillation where the teacher and the student have originally been the same model, but the student has undergone some change. The most likely place where you will see self-distillation at the moment is in RL post-training, where the objective can often look something like this:</p>

\[\begin{equation*}
\max_{\pi_\theta}
\mathbb{E}_{x \sim \mathcal{D},\, y \sim \pi_\theta(y \mid x)}
\left(
r_\phi(x, y)
- \textcolor{green}{\beta \, \mathrm{KL}\!\left(
\pi_\theta(y \mid x)\,\|\,\pi_{\mathrm{ref}}(y \mid x)
\right)}
\right)
\end{equation*}\]

<p>Here, \(r_\phi(x, y)\) is some task reward and \(\textcolor{green}{\beta \, \mathrm{KL}\!\left(\pi_\theta(y \mid x)\,\|\,\pi_{\mathrm{ref}}(y \mid x)\right)}\) is a KL divergence term penalizing drift of the model being post-trained (the student) to the model before this post-training step (the teacher). This is self-distillation. More precisely, it is <em>on-policy self-distillation</em> since the data we compute the KL on is data which is generated by the student.</p>

<p>In the case of retrofitting, we often do not even have a term like \(r_\phi(x, y)\). Our goal is purely for our model after the architecture change to perform as well as the model before the architecture change. Self-distillation is a really natural fit here. Unfortunately, using on-policy data is often tricky since it requires a fast way to sample from the student in the training loop. However, in my experience, off-policy distillation already provides huge benefits over retaining the model’s pretraining objective.</p>

\[\text{on-policy self-distillation} &gt; \text{off-policy self-distillation} \gg \text{no self-distillation}\]

<p>As an example, let’s say we want to make our model faster by quantizing it. The model should retain its original behaviour to the extent possible. We can very easily do off-policy self-distillation by choosing some dataset (for example, web-style pretraining data) and for every batch, first computing the logits of the original unquantized model (the teacher) and the quantized model (the student), then updating our quantized model via the KL divergence gradients of the student logits to the teacher logits.<d-footnote>In some cases, it makes sense to compute and store the teacher logits ahead of time. In this case, we can <a href="https://blog.character.ai/squinch">use tricks</a> to reduce the memory load from needing to store the teacher logits.</d-footnote></p>

<p>An important property of a self-distillation objective is <em>exactness</em>: the objective should be minimal if and only if the retrofitted model behaves exactly like the original model. Otherwise the optimization process admits solutions which are not our desired one. This is often trivial. For example, KL divergence is exact in this sense, irrespective of whether we are on- or off-policy. However, exactness is not always trivial: if we change the tokenizer (i.e., the student and the teacher end up using different tokenizers), we can not use standard KL. In this case, exactness of the objective is not so easy to achieve, but luckily <a href="https://arxiv.org/abs/2503.20083">we wrote a paper doing so</a>. Exactness also becomes more important to consider when we supplement our main objective such as KL with auxiliary losses which may improve performance on some task but remove exactness (which is not to say that this is necessarily bad, but it’s an important tradeoff!).</p>

<p>If you just remember one thing from this blog post, it should be to use self-distillation. This has the single biggest impact on retrofitting success in my experience, and it’s usually pretty easy to implement.</p>

<h3 id="why-self-distillation-shouldnt-the-teacher-belarger-than-the-student">Why <em>self-</em>distillation? Shouldn’t the teacher be larger than the student?</h3>

<p>Let’s say we have a larger model from the model family of which we are trying to create a retrofit available. Shouldn’t we use this model as a teacher? Potentially yes, but there is <a href="https://arxiv.org/abs/2502.08606">some evidence</a> that larger models do not always make better teachers, and a teacher which is too large can actually lead to worse performance of a student trained on it. Additionally, larger teachers require more compute. In my experience, using the model itself as the teacher via self-distillation usually provides the best tradeoff, but this may not always be the case.</p>

<h1 id="architecture-space-graduality">Architecture-Space Graduality</h1>

<p><img src="/assets/img/retrofitting_graduality.jpg" width="100%" /></p>

<p>If our change to the model architecture is too large, doing it all-at-once can catastrophically break the model. We can instead gradually move from the original model’s architecture to the target architecture. Graduality is less well-established than self-distillation, but there is growing evidence that it can be useful. For example, <a href="https://arxiv.org/abs/2507.02559">gradually removing LayerNorms can create a LayerNorm-Free model</a>, <a href="https://arxiv.org/abs/2506.05345">gradually increasing KV cache sparsity can create faster models</a> and <a href="https://arxiv.org/abs/2512.15586">byte-level models can gradually learn to compress their inputs more strongly</a>. Graduality is very interesting for a couple of reasons.</p>

<p>First, the architecture space can be quite discrete (for example, it’s not really intuitive to think of ‘half a LayerNorm’). So the effectiveness of graduality depends to a large extent on how well we can define a gradual process over this somewhat discrete space. For example, we can trivially make any LLM architecture change gradual on paper by letting our changed model consist of a copy of the original model (which is not trained), plus our changed model, and then throughout training gradually interpolating from the original model’s logits to our changed model’s logits. However, I would not expect this type of graduality to help us; the success of graduality seems in some way contingent on the ‘fine-grainedness’ with which we can interpolate between the two architectures.</p>

<p>Second, there is no free lunch through graduality. Every training step we spend on an architecture which is in-between our original architecture and the final architecture is one training step we do not spend on the final architecture. In other words, moving gradually makes our final goal less in-distribution.</p>

<p>There is an interesting connection between architecture-space graduality and <a href="https://arxiv.org/abs/2101.10382">curriculum learning</a>, where a model is trained on easier data first and training gradually moves toward harder examples. I am not aware of successful applications of curriculum learning in modern LLMs.<d-footnote>Correct me if I'm wrong! This excludes the division into pretraining, midtraining and post-training corpora, which might be called a curriculum.</d-footnote> What could make architecture-space graduality different? I believe the main reason is that architecture-space changes can be large enough to cause completely catastrophic destruction of the original LLMs’ capabilities, making graduality necessary (and worth it). In contrast, the level of ‘complete catastrophicness’ of data changes is almost never as high since the model architecture remains fully intact, making it more effective to spend as much time on the target data distribution as possible.</p>

<h1 id="expressivity">Expressivity</h1>

<p><img src="/assets/img/retrofitting_expressivity.jpg" width="100%" /></p>

<p>If your retrofit does not perform well (especially if you are already using self-distillation, plus maybe architecture-space graduality), a good question to ask is: how expressive is my changed architecture compared to the original architecture? This might inform you as to the extent you can expect the original model’s performance to be preserved, and what you could change to achieve higher performance.</p>

<p>I think of expressivity in very rough terms as ‘the class of functions that our model can represent’. For example, naively transferring a subword-level language model to the byte-level (by only changing the tokenizer) makes the embedding matrix much smaller, from on the order of 100k rows to 256 rows. In smallish models, the embedding matrix accounts for a substantial fraction of the total parameters so we might expect a naive byte-level transferred model to perform a lot worse in this case. Having fewer learnable parameters makes it less expressive. It is possible to <a href="https://arxiv.org/abs/2503.20083">devise a mechanism</a> to retain the original embedding matrix to resolve this expressivity mismatch and improve performance.</p>

<p>Let’s take another example from retrofitting to the byte-level (completely coincidentally, this is where my own work is!). We can create a competitive byte-level retrofit by introducing a two-stage hierarchy with bytes as the first level and patches of one or more bytes as the second level as we did in <a href="https://arxiv.org/abs/2512.15586">Bolmo</a>. How we group bytes into patches is quite important, and we saw that prior mechanisms to do so were less expressive than the subword tokenization mechanism used in the original model. Resolving this expressivity mismatch improved performance a lot.</p>

<p>There are also many examples outside of tokenization, for example from <a href="https://www.kevinyli.com/papers/24-neurips-mohawk.pdf">MOHAWK</a>, which replaces Attention layers with SSMs to speed up the model, keeping Attention in a couple of layers to retain sufficient expressivity and (again) quantization, which vastly reduces the theoretical expressivity but empirically <a href="https://arxiv.org/abs/2208.07339">can be designed to mostly preserve the expressivity of the space LLM computations are in</a>.</p>

<p>All in all, mismatches in the expressivity of the original and the retrofitted model can often explain performance differences. Sometimes, expressivity mismatches are desired, such as when creating a retrofit which should be faster than the original model by being less expressive. In other cases, they are bugs which we can fix to improve retrofitting performance.</p>

<h1 id="data-coverage">Data Coverage</h1>

<p><img src="/assets/img/retrofitting_data.jpg" width="100%" /></p>

<p>Dread it. Run from it. In the end you can’t escape making data choices. This is also true for retrofitting. What makes data tricky in the case of retrofitting is that we typically have a training budget which is orders of magnitude smaller than if we were to train from scratch. So we can only put so much data through the model. This can lead to worse behaviour on the tail ends of the distribution, even if we do a good job at preserving or improving performance on the bulk of the distribution’s mass.</p>

<p>I believe that self-distillation helps here by in some ways making training less liable to overfit to the small extra portion of data that the model sees during the retrofitting phase, and by implicitly transferring some amount of tail-end knowledge through the distillation objective. That said, data is still extremely important. I haven’t closely looked into data choices for retrofitting so far myself, and I’m not aware of a lot of work that does. This is a super important area though! I would love to see more work on it.</p>

<p>I’ll end with a data anecdote from <a href="https://arxiv.org/abs/2512.15586">Bolmo</a>: we didn’t explicitly prioritise maths performance, so the fraction of maths data in our mix was relatively small (but nonzero!). Unsurprisingly in hindsight, maths is the one area where in our benchmarks Bolmo is substantially behind the original model. This is still a good outcome. We have a benchmark for maths and we know that maths performance is worse. But there could be other areas where some aspect of the architecture change caused a performance degradation which we are not aware of. I would be very curious if there are effective ways to automatically identify these kinds of problem areas during training without explicit benchmarks to track them. But for now, the best we can do is to carefully design our data mix.</p>

<h1 id="conclusion">Conclusion</h1>

<p>To summarise: if you’re retrofitting a model, start with self-distillation. This is the single most impactful ingredient. Then consider whether your architecture change is drastic enough to warrant graduality, check for expressivity mismatches that might limit your ceiling, and think carefully about data coverage.</p>

<p>I’m excited to see more work on retrofitting! It can be a powerful tool complementary to training from scratch, without being nearly as expensive. If you try these ingredients on your own project, I’d love to hear how it goes.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Most of my research so far has been on changing the architecture of an existing language model after it has already been trained (called retrofitting), often via an additional training procedure. I view retrofitting as a superset of fine-tuning, where we are not only allowed to change the values of the model parameters, but also which parameters there are.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://bmin.ai/assets/img/retrofitting_header.jpg" /><media:content medium="image" url="https://bmin.ai/assets/img/retrofitting_header.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>